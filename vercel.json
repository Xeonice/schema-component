{
  "buildCommand": "pnpm -r --workspace-concurrency=1 run build && pnpm --filter storybook build",
  "outputDirectory": "packages/storybook/dist",
  "installCommand": "pnpm install",
  "framework": null
}
